apiVersion: v1
kind: ServiceAccount
metadata:
  name: document-ingestion
  namespace: document-ingestion
# Token auto-mounting disabled; the service does not call the Kubernetes API.
# AWS credentials are supplied via the document-ingestion-secrets Secret.
automountServiceAccountToken: false
